{
  "name": "game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && npm run typecheck:e2e && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "verify:assets": "node scripts/verify-assets.mjs",
    "typecheck:e2e": "tsc -p tsconfig.e2e.json",
    "qa:build": "npm run build && npm run test:e2e -- --list",
    "test:cloud": "VITE_SUPABASE_URL=http://127.0.0.1:54321 VITE_SUPABASE_PUBLISHABLE_KEY=test-key playwright test cloud-messages cloud-photos cloud-ending cloud-public-guestbook --workers=1"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0",
    "ky": "^2.1.0",
    "phaser": "^4.2.1",
    "zod": "^4.6.5"
  }
}
