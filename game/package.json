{
  "name": "game",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && npm run typecheck:e2e && vite build",
    "preview": "vite preview",
    "test:e2e": "playwright test",
    "verify:assets": "node scripts/verify-assets.mjs",
    "typecheck:e2e": "tsc -p tsconfig.e2e.json",
    "qa:build": "npm run build && npm run test:e2e -- --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^26.4.0",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  }
}
