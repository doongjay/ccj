# Batch C — 사진과 축하의 보상, 인물 연출, 완료 피드백

## 0. 이번 작업의 범위와 목표

승인된 A/A1/B를 기반으로 **Batch C: F11 / F12 / F13 / F14 / F15**를 구현한다.

목표는 기능 수를 늘리는 것이 아니라, 하객이 '내가 축하했고, 사진을 남겼고, 그 순간에 참여했다'고 느끼게 만드는 것이다.

B 검수에서 남긴 두 항목을 이번 작업에 포함한다.
- **B-E01:** 회귀 테스트 요약 40개와 상세 24개의 증빙 차이 보완.
- **B-V01:** 포토부스의 진행 안내가 돌아가기 버튼 아래를 덮는 문제. F12에서 함께 해결.

별도 B.1 검수 대기 없이 위 보완과 C를 한 번의 작업 흐름으로 진행한다. D/F24/F25는 시작하지 않는다.

이 문서를 처음부터 끝까지 읽고 실제 현재 구현을 확인한다. 과거 리뷰의 파일명과 내부 함수는 탐색 힌트이며 현재 코드를 확인하지 않은 채 존재/구조를 가정하지 않는다.

## 1. 보호해야 하는 기준

### 사용자 작업과 소스

- 기존 커밋되지 않은 변경을 보존한다. reset/clean/stash 또는 무관한 대규모 rewrite를 하지 않는다.
- 작업 전 상태와 이번 변경을 구분해 기록한다.
- 실제 예식 정보, 실사 사진, 이름, 하객측별 필수 활동, 이동 경로, 식사 순서, 방명록 저장 의미를 임의로 바꾸지 않는다.
- 외부 메시지/공유/전송은 실행하지 않는다. QA 이름과 로컬 테스트 데이터만 사용한다.

### 이미 승인된 UX

- A/A1: 간결한 첫 로비 안내, 하트 진행도, 정보형 수첩, 두 버튼 식장 알림, 패널 내부 닫기.
- F03: 게임 스타일의 validation, 접근 가능한 오류/포커스.
- F04/F06: 커플·도로를 드러내는 구도, 실제 표시 기준 가독성/터치 크기, 가로 화면 대안.
- B: 바닥/발 앵커 기반 이동, 안내 정보, 언제든 청첩장 접근, 메시지 건너뛰기, 단계별 로딩, 키보드 완주.
- 수첩을 다시 대형 목적지 버튼 목록으로 만들거나 필수 활동을 새로 추가하지 않는다.

### 아트와 성능

- 크림·골드·딥그린, 픽셀 프레임, Galmuri, 기존 미니미 얼굴/머리/옷을 유지한다.
- 기존 배경/실사 사진을 새로 생성하거나 일괄 리사이즈하지 않는다. 억지 smoothing이나 사진 픽셀화를 하지 않는다.
- 초기 전체 응답 baseline은 **3,507,312 bytes**, 에셋만은 **3,094,744 bytes**다. 새 연출/사진/군중 리소스는 해당 장면에서 준비한다.
- 첫 화면 5 MB 이하를 유지하고 이전 대비 증감 이유를 기록한다. 예식장 준비 때문에 전체 게스트·사진 에셋을 다시 boot에서 생성하지 않는다.
- 새로운 연출은 소리 없이도 이해돼야 한다. 새 오디오 시스템, BGM, 전체 효과 설정창은 이번 범위가 아니다.
- 새/변경 연출은 기존 reduced-motion 설정을 존중한다. 이것은 D의 전역 F17 작업을 통째로 시작하라는 뜻이 아니다.

## 2. B-E01 — 증빙 정합성 먼저 확보

기존 `docs/game-review/after-batch-b/regression-results.json`은 요약 expected=40, 상세 tests=24다.

1. 기존 raw reporter/로그가 있으면 원본을 보존하고 어디에서 항목이 생략됐는지 기록한다.
2. 원본이 없으면 당시 결과를 재구성한 것처럼 만들지 않는다. 누락을 명시한다.
3. C 완료 후 현재 소스 기준으로 관련 전체 회귀를 실행해 **모든 테스트 개별 결과**를 제출한다.
4. 요약과 상세를 집계 단위(테스트, 프로젝트, 재시도)별로 구분하여 대조한다. 테스트 항목은 생략하지 않는다.
5. 실패 후 성공한 재시도, skipped, flaky와 fixture 변경을 숨기지 않는다.
6. 테스트를 구현에 맞게 변경했다면 '동등하거나 더 강한 사용자 관점 검증'이 유지되는지 설명한다. 최소 크기·핵심 행위·상태 불변식을 낮춰 통과시키지 않는다.

이미지 base64를 제거하는 것은 허용한다. 실패/재시도/테스트 이름/개별 상태까지 제거하는 것은 허용하지 않는다. B-E01은 이 자료가 확보되기 전까지 완료 표시하지 않는다.

## 3. F11 — 박수와 환호에 다른 반응 만들기

### 현재 문제와 방향

기존 리뷰에서 두 선택이 같은 단체사진으로 즉시 연결돼 선택한 행동이 드러나지 않았다. 현재 코드에서 이 상태를 먼저 재확인한다.

공통 흐름:
`선택 → 선택한 행동이 보임 → 커플/주변의 짧은 응답 → 기존 단체사진 준비`

**박수**
- 기존 스타일과 호환되는 짧은 박수 포즈/손동작 또는 작은 박수 표현을 사용한다.
- 주변 하객이나 커플의 짧은 반응으로 행동이 전달되게 한다.

**환호**
- 플레이어 위치와 연결된 작은 `축하해!` 말풍선과 구별되는 포즈/응답을 사용한다.
- 전체 화면을 덮는 대사창을 하나 더 추가하는 방식은 피한다.

### 구현 제약

- 0.8~1.5초 정도를 초기 연출 기준으로 삼되, 실제 화면에서 인지 가능한 시간을 확인한다. 임의 시간 수치를 성공 근거로 삼지 않는다.
- 원래 선택 화면의 커플을 계속 보이게 한다. 얼굴 위에 말풍선/마커를 덮지 않는다.
- 새 팔/손 때문에 의상·헤어 합성이 깨지지 않게 대표 외형을 검증한다. 조악한 새 캐릭터를 별도로 만들지 않는다.
- 빠른 연타/Enter 반복이 행동을 두 번 실행하거나 반응을 한 프레임 만에 없애지 않아야 한다.
- 청첩장을 중간에 열면 반응과 타이머가 중단되고 복귀 후 한 번만 이어져야 한다.
- reduced-motion에서는 정적 포즈/짧은 텍스트로 구별해도 된다. 축하 결과는 같아야 한다.

### 완료 기준

- 소리 없이 봐도 어느 선택을 했는지 두 캡처/짧은 영상에서 구별된다.
- 두 선택 모두 동일한 정상 단체사진 흐름으로 정확히 한 번 연결된다.
- 신랑측/신부측, 키보드/포인터 모두 작동한다.
- 화면 흔들림, 전체 컨페티, 새 점수/보상 수치는 추가하지 않는다.

## 4. F12 + B-V01 — 촬영 결과를 감상할 수 있게 만들기

### 현재 문제

포토부스/신부대기실에서 촬영하고 자동 복귀하여 결과를 즐길 시간이 부족했다.
또한 B의 `regressions/photo-booth-pose-393x852.png`에서 하단 `화면을 눌러 계속 · 잠시 후 자동 진행` 안내가 `로비로 돌아가기` 버튼 하단을 덮는다.

### 원하는 결과

두 사진 활동에 아래 흐름을 적용한다.

`촬영 전 → 이동/포즈/촬영 중 → 사진 결과 감상 → 명시적인 로비 복귀`

- 기존 장면과 실제 선택한 미니미를 담은 작은 픽셀 사진 카드/액자를 결과로 보여준다.
- 포토부스는 개인 기념사진, 신부대기실은 인사와 함께 찍은 사진으로 구별한다.
- 텍스트 예: `추억을 수첩에 남겼어요 ♥`.
- 결과는 사용자가 `로비로 돌아가기`를 누를 때까지 유지한다. 자동 복귀 타이머를 남기지 않는다.
- 축하 텍스트/프레임을 적절히 사용하되 확인 버튼을 여러 단계로 늘리지 않는다.

### 결과물과 상태

- 사진에 현재 미니미의 성별/얼굴/안경/헤어/의상이 정확히 반영되어야 한다.
- 결과 사진에는 DOM 버튼, 시스템 포커스, 청첩장 버튼, 디버그 마커, 타이머 안내가 찍히지 않게 한다.
- 같은 세션의 수첩에서 완료한 촬영을 작은 썸네일 또는 읽기 전용 사진으로 다시 볼 수 있게 한다. 기존 일정/하트 정보가 사라지거나 거대한 앨범으로 대체되면 안 된다.
- 이번에는 작고 유한한 세션 내 데이터만 사용한다. 영속적 앨범/서버 업로드/다운로드·공유 기능은 만들지 않는다. F18 저장복구와 혼동하지 않는다.
- 사진 결과/texture를 매 프레임 생성하거나 모든 장면의 고해상도 이미지를 base64로 localStorage에 넣지 않는다. 재시작·장면 종료 시 수명을 관리한다.
- 실제 촬영이 완료되는 시점에 진행도를 한 번만 갱신한다. 촬영 전 복귀는 완료로 처리하지 않는다.
- 재방문/다시 감상/수첩 열기 때문에 진행도나 사진이 중복 증가하지 않는다.

### 컨트롤 배치 및 B-V01

- 한 상태에서 무엇을 눌러야 하는지 하나의 일관된 안내만 보여준다.
- 결과 화면에서는 `잠시 후 자동 진행`을 표시하지 않는다.
- `돌아가기`·촬영 액션·타이핑 완료/다음 안내가 동시에 서로 가려서는 안 된다.
- 보이지 않거나 사용 불가한 컨트롤은 포인터와 Tab 경로에서도 비활성화한다. 필요한 대체 키보드 조작은 남긴다.
- CTA와 안내의 표시 영역/터치 영역이 320/393/430 폭에서 겹치지 않음을 확인한다.
- 정보창처럼 배경 입력을 막고, 키보드 복귀/청첩장 복귀 시 포커스를 정상 복원한다.

### 완료 기준

- 두 사진 결과를 최소 5초 동안 아무 입력 없이 둬도 자동 복귀하지 않는다. 시험의 5초는 제품의 표시 제한 시간이 아니다.
- 촬영 결과가 실제 외형과 일치하며 인물이 구별된다.
- 촬영 전 취소, 촬영 완료, 결과 감상, 수첩 재열람, 재방문에서 진행도 갱신이 정확하다.
- 중간 청첩장 열기/닫기와 빠른 입력으로 중복 전환/유령 타이머가 발생하지 않는다.
- B-V01에 해당하는 하단 겹침이 없어졌다.

## 5. F13 — 사진 속 인물 크기와 바닥 접지감

### 방향

로비 전체를 확대하는 작업이 아니다. 사진을 남기는 순간에 사용자가 고른 미니미와 커플이 알아볼 수 있게 한다.

- 포토부스/신부대기실은 접근 중에는 기존 넓은 구도를 유지할 수 있다.
- 결과 감상 순간에는 인물 중심의 가까운 구도/크롭/액자 구성을 사용한다.
- 화면 대부분이 바닥이고 인물은 작은 구석에 남는 결과를 피한다.
- 미니미 발 아래 장면에 맞는 작은 픽셀 접지 그림자를 사용할 수 있다. 흐린 CSS 그림자/글로우를 모든 캐릭터에 일괄 적용하지 않는다.
- 예식장 커플이 시각적 중심이고 플레이어는 참여자로 남아야 한다.

### 기술 경계

- 프레젠테이션용 크롭/배치와 이동 판정용 발 앵커를 분리한다. F02의 좌표/clearance를 카메라 연출 때문에 바꾸지 않는다.
- 부드럽게 확대하며 픽셀이 계속 출렁이는 연출보다 안정된 최종 구도를 우선한다.
- 기존 픽셀 샘플링을 유지한다. F24 전역 픽셀 밀도 정비는 하지 않는다.
- 카메라/컨테이너 변형과 추가 그림자/마커는 장면 종료/재방문 시 초기화한다.
- 기존 인물의 머리/발이 잘리거나 의자/꽃/벽 위로 떠 보이지 않게 한다.

### 완료 기준

- 320/393/430 폭 결과 화면에서 고른 헤어·옷, 함께 찍는 인물을 구별할 수 있다.
- 전후 비교는 같은 외형·장면 단계·viewport로 한다.
- 로비 이동 크기/충돌 기준은 유지된다.
- 사진 장면을 여러 번 왕복해도 확대/그림자/마커가 누적되지 않는다.

## 6. F14 — 단체사진 게스트 다양성과 플레이어 식별

### 방향

과거 G16은 후보 42개에 `(index * 7) % 42`가 적용돼 6개만 반복된다고 보고했다. 현재 후보 목록과 선택 코드를 확인하고 실제 결함에 맞게 고친다. 현재 개수까지 무조건 42로 고정하지 않는다.

- 시드가 고정된 중복 없는 섞기 또는 동등한 방식으로 기존 후보를 골고루 쓴다.
- 슬롯보다 후보가 적다면 한 순환을 사용한 후에만 재사용하고, 가까운 슬롯에 동일 외형이 연속하지 않게 한다.
- 플레이어 바로 옆의 placeholder 하객이 플레이어와 정확히 같은 외형을 사용하지 않게 한다.
- 처음 구도가 보일 때 플레이어 근처에 작은 `나` 표시/짧은 프레임을 제시할 수 있다. 얼굴을 가리거나 항상 깜박이면 안 된다.
- 실제 결과 사진에서는 식별 표시를 자연스럽게 정리하여 사진을 망치지 않는다.
- 기존 신랑측/신부측 위치와 커플의 우선순위를 유지한다.

### 범위 주의

이 작업의 대상은 예식 연출용 placeholder 군중이다. 실제 저장된 방명록 하객을 랜덤화하거나 삭제하지 않는다. 청첩장 하객 페이지/실사 갤러리 동작을 바꾸지 않는다.

군중 texture는 B의 예식 단계에서 준비한다. boot 전체생성으로 되돌리지 않는다.

### 완료 기준

- 기록에 후보 수, 선택 인덱스/프로필 키, 표시 슬롯 수, 고유 외형 수를 남긴다. 단순히 '랜덤 적용 완료'라고 보고하지 않는다.
- 같은 시드로 재현되고, 고정된 일부 후보만 반복되던 문제가 해결된다.
- 근접 하객끼리 똑같아 보이는 군집이 줄고, 플레이어가 즉시 구별된다.
- 두 하객측의 위치/외형, 기존 카운트다운/다음 버튼과 키보드 경로가 유지된다.

## 7. F15 — 접수 완료는 잠깐 알리고 수첩에 남기기

- 접수가 실제로 처음 완료될 때만 짧은 토스트/알림을 보여준다.
- 표시 시간은 약 1.5~2.5초를 시작점으로 삼고 읽을 수 있는지 확인한다. 자동 사라짐 후에도 수첩 진행도는 계속 보인다.
- 로비 재생성 때마다 완료 문구를 다시 띄우지 않는다.
- 플레이어 발/이동 경로, 수첩, 청첩장 버튼을 덮지 않는다.
- 반복 토스트를 만들거나 알림이 끝날 때까지 이동을 강제 차단하지 않는다.
- 청첩장 중간 열기/닫기, 타 방 방문 후 복귀에서 완료 알림이 중복되지 않는다.
- 가능하면 기존 안내 방식에 맞춘 짧은 접근 가능한 완료 알림을 사용하고, 프레임마다 반복 낭독하지 않는다.

완료 기준: 첫 완료는 명확하게 인지되고 이후 로비를 재방문하면 영구 배너 없이 진행도가 유지된다.

## 8. 구현·검증 순서

1. 현재 소스 및 기존 상태/진행·로딩·접근성 코드를 확인한다. B-E01 기록을 점검한다.
2. 사진 상태별 컨트롤 정리와 결과 화면(F12/B-V01)을 구현한다.
3. 같은 사진 화면의 인물 구도/접지(F13)를 다듬는다.
4. 예식 선택의 다른 반응(F11)을 구현한다.
5. 단체사진 후보 선택/플레이어 식별(F14)을 수정한다.
6. 접수 완료 알림(F15)을 정리한다.
7. source 편집을 멈춘 뒤 동일 스냅샷으로 production build, 전체 관련 회귀, 시각·시간 흐름 검증을 수행한다.
8. 증빙과 ZIP을 생성하고 멈춘다. 중간 단계마다 사용자에게 캡처를 전달하거나 승인받을 필요는 없다.

## 9. 필수 검증

### 전체 여정

- 신부측 + 자차: 필수 촬영 포함, 식사 먼저, 한 예식 반응, 메시지 저장, 청첩장/재시작.
- 신랑측 + 지하철: 필수 활동, 예식 먼저, 다른 예식 반응, 메시지 건너뛰기, 청첩장/재시작.
- 두 여정 각각 포인터와 키보드-only로 검증한다. 키보드-only를 강제 focus나 직접 상태 변경으로 대신하지 않는다.
- 오답 복귀, 중간 청첩장 복귀, A/A1 수첩/닫기/validation, 바닥 충돌 검증을 유지한다.

### 사진/시간 경계

- 촬영 전 복귀, 촬영 완료, 결과 5초 이상 유지, 결과 복귀, 수첩 재열람, 반복 방문.
- 박수/환호 반응 중 청첩장 열었다 닫기.
- 사진 결과 감상 중 청첩장 열었다 닫기.
- 빠른 연타/키 반복에도 진행도·장면 전환·사진 생성이 중복되지 않는다.
- reduced-motion에서도 축하/촬영 완료를 이해할 수 있다.
- B-V01처럼 서로 다른 오버레이/CTA가 겹치는 상태를 검사한다.

### 크기·성능

- 320×568 / 393×852 / 430×932, desktop 1440×900 DPR 1, 기존 가로화면 대안.
- 새 사진 결과와 반응은 393 폭을 기본으로, 320 폭에서도 확인한다. DPR 3 대표 캡처도 남긴다.
- production cold-cache를 동일 방법으로 재측정한다. 전체 응답/게임 에셋을 따로 쓰고 B baseline과 비교한다.
- 누적 사진/군중 texture 수 또는 생성 수를 반복 방문 전후로 확인한다. 항상 증가하는 캐시/타이머가 없도록 한다.
- page/console/asset/missing-frame 오류를 기록한다. 의도적 실패 주입은 정상 경로 오류와 분리한다.

## 10. AFTER 증빙 — 스크린샷만으로 동작을 통과시키지 않기

저장: `docs/game-review/after-batch-c/`
기존 BEFORE/A/A1/B 증빙은 덮어쓰지 않는다.

### 스크린샷

최소 아래 상태를 남긴다. 파일명에 실제 viewport를 표기한다.

- `f11-applause-response-393.png`
- `f11-cheer-response-393.png`
- `f12-booth-result-393.png`
- `f12-bridal-result-393.png`
- `f12-notebook-keepsakes-393.png`
- `f12-booth-controls-no-overlap-393.png` (기존 B-V01 상태와 비교 가능하게)
- `f13-booth-result-320.png`
- `f13-bridal-result-320.png`
- `f13-photo-result-430.png`
- `f14-group-bride-393.png`
- `f14-group-groom-393.png`
- `f15-reception-acknowledged-393.png`
- `f15-lobby-revisit-no-banner-393.png`
- `keyboard-photo-result-desktop.png`
- `reduced-motion-response-393.png`

같은 외형·같은 단계의 B BEFORE가 없으면 수정 전 해당 상태를 별도로 캡처한다. 서로 다른 순간의 이미지를 전후 비교라며 제시하지 않는다.

### 짧은 실제 시간 영상

반응/사진/타이밍을 검수할 수 있도록 MP4 또는 WebM 클립을 포함한다.

- `f11-applause.webm`: 선택부터 반응, 단체사진 진입까지.
- `f11-cheer.webm`: 같은 조건의 다른 선택.
- `f12-booth.webm`: 촬영부터 결과 5초 이상 감상, 명시적 복귀까지.
- `f12-bridal.webm`: 촬영부터 결과 5초 이상 감상, 명시적 복귀까지.
- `f15-reception.webm`: 완료 표시, 사라짐, 진행도 유지.

393×852 정도의 실제 플레이 영역을 기록한다. 테스트 타이머 가속/순간이동/진행도 주입으로 실제 체감 연출을 꾸미지 않는다.
영상 기록이 불가능하면 불가 사유, 실제 시간 기반 연속 프레임, 이벤트 타임스탬프 로그로 대체하고 '영상 검증 완료'라고 적지 않는다.

### README 및 원본 기록

`README.md`에 다음을 포함한다.
- F11~F15, B-E01/B-V01 각각 PASS / PARTIAL / BLOCKED 및 근거 경로.
- 정확한 파일 변경 목록, 기능/연출 변경 설명.
- 테스트 명령, 실행 환경, 실행 시각, commit SHA와 미커밋 변경을 식별할 수 있는 fingerprint.
- 동일 소스로 수행한 모든 테스트의 상세/요약 대조. 생략한 테스트·미실행 플랫폼은 명시한다.
- B-E01의 기존 기록 보강 결과. 과거 원본이 없는 경우 그 사실과 새 검증을 구분한다.
- production bytes 표, 실제 로딩 시간과 에뮬레이션 여부 구분.
- 반응 시작/종료·사진 완료/복귀 시각, 진행도 갱신 횟수, 군중 후보/고유 수, 반복 방문의 리소스 상태.
- 영상/스크린샷 목차. 정지 이미지로만 판단한 것과 실제 입력/타이밍으로 확인한 것을 구분한다.
- 테스트 fixture/assertion 수정 내역과 이유.
- 승인된 A/A1/B 유지 및 D/F24/F25 미착수 여부. 불가피한 공유 코드 변경은 무엇을 재검증했는지 적는다.

JSON reporter, 관련 원본 로그, 성능 측정 JSON도 함께 넣는다. 실패/재시도는 삭제하지 않는다.

## 11. 인계와 종료

위 폴더를 **`docs/game-review/after-batch-c.zip`**으로 압축한다.
압축에 README, 이미지, 영상/대체 연속 프레임, JSON/로그가 포함됐는지와 무결성을 확인한다.
`node_modules`, 빌드 전체, 기존 배치 전체, `__MACOSX`, `.DS_Store`, 비밀정보는 넣지 않는다.

마지막 출력:
1. 완료/미완료 F-ID 및 B-E01/B-V01.
2. 테스트 상세와 요약 수가 맞는지.
3. 첫 화면 전체 bytes와 B 대비 증감.
4. 대표 동작 영상/대체 증빙이 포함됐는지.
5. ZIP 경로와 크기.

**여기서 멈춘다. Batch D/F24/F25는 진행하지 않는다.**


## 구현 및 검증 완료 — 2026-09-14

- [x] fixed — F11; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [x] fixed — F12; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [x] fixed — F13; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [x] fixed — F14; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [x] fixed — F15; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [x] fixed — B-E01; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [x] fixed — B-V01; 전체 acceptance PASS. [증빙](docs/game-review/after-batch-c/README.md)
- [ ] remaining — Batch D / F24 / F25: 이번 작업에서 시작하지 않음.

원래 발견 내용과 기준은 삭제하지 않았다. 기존 A/A1/B를 보존했다. 최종 91개 논리 테스트는 적용 가능한 환경에서 모두 통과했고, dev 91 + production 14의 105개 프로젝트 결과/시도는 102 PASS / 3 production-only SKIP / 0 FAIL / 0 FLAKY다. 과거 B의 누락된 16개 결과를 재구성하지 않았다. 별도의 기존 asset manifest 검사에는 미등록 이미지 40건이 남아 있으며 자세한 한계와 실패 기록을 README에 공개했다.
